package com.google.android.gnd.persistence.local.room.entity;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.room.Embedded;
import com.google.android.gnd.persistence.local.room.models.UserDetails;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_AuditInfoEntity extends AuditInfoEntity {

  @Embedded(prefix = "user_")
  @NonNull
  private final UserDetails user;

  @NonNull
  private final Long clientTimestamp;

  @Nullable
  private final Long serverTimestamp;

  private AutoValue_AuditInfoEntity(
      UserDetails user,
      Long clientTimestamp,
      @Nullable Long serverTimestamp) {
    this.user = user;
    this.clientTimestamp = clientTimestamp;
    this.serverTimestamp = serverTimestamp;
  }

  @Embedded(prefix = "user_")
  @NonNull
  @Override
  public UserDetails getUser() {
    return user;
  }

  @NonNull
  @Override
  public Long getClientTimestamp() {
    return clientTimestamp;
  }

  @Nullable
  @Override
  public Long getServerTimestamp() {
    return serverTimestamp;
  }

  @Override
  public String toString() {
    return "AuditInfoEntity{"
        + "user=" + user + ", "
        + "clientTimestamp=" + clientTimestamp + ", "
        + "serverTimestamp=" + serverTimestamp
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof AuditInfoEntity) {
      AuditInfoEntity that = (AuditInfoEntity) o;
      return this.user.equals(that.getUser())
          && this.clientTimestamp.equals(that.getClientTimestamp())
          && (this.serverTimestamp == null ? that.getServerTimestamp() == null : this.serverTimestamp.equals(that.getServerTimestamp()));
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= user.hashCode();
    h$ *= 1000003;
    h$ ^= clientTimestamp.hashCode();
    h$ *= 1000003;
    h$ ^= (serverTimestamp == null) ? 0 : serverTimestamp.hashCode();
    return h$;
  }

  static final class Builder extends AuditInfoEntity.Builder {
    private UserDetails user;
    private Long clientTimestamp;
    private Long serverTimestamp;
    Builder() {
    }
    @Override
    public AuditInfoEntity.Builder setUser(UserDetails user) {
      if (user == null) {
        throw new NullPointerException("Null user");
      }
      this.user = user;
      return this;
    }
    @Override
    public AuditInfoEntity.Builder setClientTimestamp(Long clientTimestamp) {
      if (clientTimestamp == null) {
        throw new NullPointerException("Null clientTimestamp");
      }
      this.clientTimestamp = clientTimestamp;
      return this;
    }
    @Override
    public AuditInfoEntity.Builder setServerTimestamp(@Nullable Long serverTimestamp) {
      this.serverTimestamp = serverTimestamp;
      return this;
    }
    @Override
    public AuditInfoEntity build() {
      String missing = "";
      if (this.user == null) {
        missing += " user";
      }
      if (this.clientTimestamp == null) {
        missing += " clientTimestamp";
      }
      if (!missing.isEmpty()) {
        throw new IllegalStateException("Missing required properties:" + missing);
      }
      return new AutoValue_AuditInfoEntity(
          this.user,
          this.clientTimestamp,
          this.serverTimestamp);
    }
  }

}
